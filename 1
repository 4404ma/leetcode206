        # real_head = prehead = head
        # while head.next != None:
        #     prehead = head
        #     head = head.next
        #     return self.reverseList(head)
        # head.next = prehead
        # return prehead
